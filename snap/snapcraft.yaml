name: notepod
version: 0.2.44
summary: Privacy preserving note taking app using PODs.
description: |
  Notepod is a note taking app, storing your notes within your Solid
  Pod. Make notes anywhere, anytime, on any device and review them
  later on. Great for that bush walk where you've suddenly come up
  with a great idea you don't want to lose, or sharing the shopping
  list with the family so whoever is next at the shops has it at their
  finger tips.

base: core22
grade: stable
confinement: strict

apps:
  notepod:
    command: notepod
    extensions: [gnome] # gnome includes the libraries required by flutter
    plugs:
      - desktop
      - desktop-legacy
      - wayland
      - x11
      - opengl
      - network
      - home

# For flutter_secure_storage and vault_storage we need
# `libsecret-1-dev` and `libjsoncpp-dev` to build and `libsecret-1-0`
# and `libjsoncpp1` to run. These need to be included for `solidpod`
# based apps. See
#
# https://github.com/juliansteenbakker/flutter_secure_storage/blob/develop/README.md
# https://pub.dev/packages/vault_storage/versions/2.2.0#platform-setup

parts:
  notepod:
    source: .
    plugin: flutter
    flutter-target: lib/main.dart
    build-packages:
      - clang
      - cmake
      - ninja-build
      - libgtk-3-dev
      - libblkid-dev
      - liblzma-dev
      - libsecret-1-dev
      - libjsoncpp-dev
    stage-packages:
      - libsecret-1-0
      - libjsoncpp-dev
